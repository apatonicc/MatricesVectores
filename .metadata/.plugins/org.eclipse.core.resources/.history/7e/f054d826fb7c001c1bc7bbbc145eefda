/**
 * Dadas las declaraciones
final int NUM_DPTOS = 100;
final int NUM_TIENDAS = 10;
final int NUM_MESES = 12;
a) Declara una matriz ventas de valores en doble precisión, indexada por el número de
departamentos, tiendas y meses.
b) Crea una instancia de la matriz ventas.
c) ¿Cuál es el valor de los componentes de la matriz recién creada?
12000
d) Escribe un fragmento de código que calcule la suma de las ventas de enero.
e) Escribe un fragmento de código que calcule la suma de las ventas de la tienda 2.
f) Escribe un fragmento de código que calcule la suma de las ventas del departamento 33.
 * @author alu
 *
 */
class Ejercicio_6 {

	public static void main(String[] args) {
		final int NUM_DPTOS = 100;
		final int NUM_TIENDAS = 10;
		final int NUM_MESES = 12;
		
		int[][][] ventas = new int[NUM_DPTOS][NUM_TIENDAS][NUM_MESES];
		ventas[1][0][0] = 5;
		ventas[0][1][0] = 5;
		ventas[0][0][1] = 1;
		
		System.out.println(getSalesByMonth(ventas,2));
		
	}
	public static int getSalesByMonth(int[][][] datos,int mes) {
		int ventasMes = datos[0][0][mes];
		for(int i = 0; i < datos.length; ++i) {
			for(int j = 0; j < datos[i].length; ++j) {
				if (datos[i][j][mes] > 0)
					ventasMes += datos[i][j][mes];
			}
		}
		return ventasMes;
	}

}
